CXX_STD = CXX11
CXX=clang++
PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS)
PKG_LIBS = $(SHLIB_OPENMP_CFLAGS) $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)

SOURCES = Sample_IW.c
OBJECTS = Sample_IW.o
PKG_CFLAGS = -I.

all: $(SHLIB)

clean:
	@rm -f $(OBJECTS)
